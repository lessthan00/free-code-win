# Free-Code 使用说明

## 0. 第一次使用之前

从网上下载的脚本会被 Windows 锁定，需要先解锁。

**方式一**：双击运行 `unblock.bat`，会自动解锁所有 ps1 脚本。

**方式二**：手动操作 — 分别右键 `install.ps1`、`start.ps1` → **属性** → 勾选 **解除锁定** → 确定。

如果解锁后仍然无法运行 .ps1，以管理员身份打开 PowerShell 执行：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

## 1. 安装环境（仅需一次）

打开 PowerShell（不是 CMD），进入项目目录，运行：

```powershell
.\install.ps1
```

选择 `[4] 全部执行`，会自动完成：
- 安装 Bun 运行时
- 安装项目依赖
- 注册 `freecode` 全局命令
- 清理旧版残留

安装完成后**重启终端**（或执行 `. $PROFILE`），之后在任意目录输入 `freecode` 即可启动。

---

## 2. 日常启动

或安装全局命令后：
```powershell
freecode             # 任意目录
```

菜单只有 4 项：
```
[1] 启动 free-code
[2] 构建 free-code
[3] 从源码运行
[4] 退出
```

---

## 3. 启动后：配置代理（翻墙）

访问国外 AI API 需要代理。启动 app 后输入：

```
/proxy set http://127.0.0.1:7890
```

常见代理软件默认端口：

| 代理软件 | 地址 |
|----------|------|
| Clash / Clash Verge | `http://127.0.0.1:7890` |
| V2Ray / v2rayN | `http://127.0.0.1:10809` |
| Shadowsocks | `http://127.0.0.1:1080` |

命令：

```
/proxy             查看当前代理
/proxy set <url>   设置代理
/proxy clear       清除代理
```

代理会自动保存，**下次启动无需重新设置**。

---

## 4. 启动后：选择 AI 提供商

### 4.1 DeepSeek

在 [platform.deepseek.com](https://platform.deepseek.com) 注册获取 API Key，然后：

```
/provider deepseek sk-xxxxxxxxxxxxxxxx
# 重启会话
```

### 4.2 Gemini

配置代理
在 [aistudio.google.com](https://aistudio.google.com) 获取 API Key：

```
/provider gemini xxxxxxxxxxxxxxxx
# 重启会话
```

### 4.3 OpenAI / Codex

配置代理
在获取 API Key：

```
/provider openai xxxxxxxxxxxxxxxx
/model ->

```

### 查看和切换

地址栏输入 %APPDATA%\ClaudeCode\settings.json
查看 模型 代理 api-key

---

## 5. 常用命令

| 命令 | 用途 |
|------|------|
| `/provider` | 查看/切换 AI 提供商 |
| `/proxy` | 查看/设置/清除代理 |
| `/model` | 选择模型 |
| `/help` | 查看帮助 |
| `/config` | 打开配置面板 |
| `/clear` | 清除对话历史 |

---

## 6. 全新小白 5 分钟上手

```
1. 双击 unblock.bat
2. PowerShell → .\install.ps1 → [4]
3. 关掉重开 PowerShell → freecode → [1]
4. /proxy set http://127.0.0.1:7890
5. /provider deepseek sk-你的key
6. 开始对话
```

---

## 7. 权限配置

项目根目录的 `settings.local.json` 预设了安全的工具权限。如果想让它全局生效（所有项目共用），复制到全局配置目录：

Windows 上全局配置目录是 `%APPDATA%\ClaudeCode\`。

**方式一**（在 PowerShell 中执行）：
```powershell
if (-not (Test-Path "$env:APPDATA\ClaudeCode")) {
    New-Item -ItemType Directory -Path "$env:APPDATA\ClaudeCode" -Force
}
Copy-Item ".\settings.local.json" "$env:APPDATA\ClaudeCode\settings.json"
```

**方式二**（在文件资源管理器中）：
1. 地址栏输入 `%APPDATA%\ClaudeCode` 回车
2. 如果没这个文件夹就新建
3. 把项目里的 `settings.local.json` 复制进去
4. 重命名为 `settings.json`
